{% extends 'base.html.twig' %}

{% block title %}Service Details{% endblock %}

{% block stylesheets %}
    <style>
        body {
            font-family: 'Barlow', sans-serif;
            background-color: #f9f9f9;
        }

        h1 {
            text-align: center;
            font-family: 'Rajdhani', sans-serif;
            color: #D92332;
            margin-top: 20px;
            margin-bottom: 40px;
        }

        .service-details {
            width: 60%;
            margin: 0 auto;
            background-color: white;
            padding: 30px;
            border-radius: 10px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
        }

        .service-details table {
            width: 100%;
            border-collapse: collapse;
            margin-bottom: 20px;
        }

        .service-details th, .service-details td {
            text-align: left;
            padding: 12px 15px;
            border-bottom: 1px solid #ddd;
        }

        .service-details th {
            width: 30%;
            color: #333;
        }

        .service-details td {
            font-weight: bold;
        }

        .service-details img {
            width: 100px;
            border-radius: 8px;
        }

        .btn-back, .btn-edit, .btn-delete {
            display: inline-block;
            text-align: center;
            padding: 10px 20px;
            border-radius: 5px;
            font-size: 16px;
            margin: 10px;
            text-decoration: none;
            font-weight: bold;
        }

        .btn-back {
            background-color: #262526;
            color: white;
        }

        .btn-back:hover {
            background-color: #333;
        }

        .btn-edit {
            background-color: #28a745;
            color: white;
        }

        .btn-edit:hover {
            background-color: #218838;
        }

        .btn-delete {
            background-color: #e74c3c;
            color: white;
        }

        .btn-delete:hover {
            background-color: #c0392b;
        }

        .actions {
            text-align: center;
        }
    </style>
{% endblock %}

{% block body %}
    <h1>Service Details</h1>

    <div class="service-details">
        <table>
            <tbody>
                <tr>
                    <th>Id</th>
                    <td>{{ service.id }}</td>
                </tr>
                <tr>
                    <th>Nom</th>
                    <td>{{ service.nom }}</td>
                </tr>
                <tr>
                    <th>Description</th>
                    <td>{{ service.Description }}</td>
                </tr>
                <tr>
                    <th>Prix</th>
                    <td>{{ service.Prix }} €</td>
                </tr>
                <tr>
                    <th>Image</th>
                    <td><img src="{{ asset('images/' ~ service.image) }}" alt="{{ service.nom }}"></td>
                </tr>
            </tbody>
        </table>

        <div class="actions">
            <a href="{{ path('app_service_index') }}" class="btn-back">Back to list</a>
            <a href="{{ path('app_service_edit', {'id': service.id}) }}" class="btn-edit">Edit</a>
            {{ include('service/_delete_form.html.twig', {'class': 'btn-delete'}) }}
        </div>
    </div>
{% endblock %}
