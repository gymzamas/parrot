{% extends 'base.html.twig' %}

{% block title %}Hello AdminCommentController!{% endblock %}

{% block body %}
    <div class="container py-5">
        <h1 class="text-center mb-4">Hello {{ controller_name }}! ✅</h1>
        <p class="lead text-center">This friendly message is coming from:</p>
        
        <div class="row justify-content-center">
            <div class="col-md-8">
                <ul class="list-group">
                    <li class="list-group-item">
                        <strong>Your controller:</strong>
                        <code>C:/Users/jtcp9/garage_parrot/src/Controller/AdminCommentController.php</code>
                    </li>
                    <li class="list-group-item">
                        <strong>Your template:</strong>
                        <code>C:/Users/jtcp9/garage_parrot/templates/admin_comment/index.html.twig</code>
                    </li>
                </ul>
            </div>
        </div>
    </div>

    <style>
        /* Simplified and improved CSS */
        .container {
            max-width: 900px;
            margin: 0 auto;
            padding: 40px;
        }

        h1 {
            font-size: 2.5rem;
            color: #333;
        }

        .lead {
            font-size: 1.25rem;
            color: #555;
        }

        code {
            background-color: #f8f9fa;
            padding: 2px 8px;
            border-radius: 4px;
        }

        .list-group-item {
            font-size: 1rem;
            border: 1px solid #ddd;
            background-color: #fff;
        }

        .list-group-item code {
            font-size: 0.875rem;
            word-wrap: break-word;
        }

        @media (max-width: 768px) {
            h1 {
                font-size: 2rem;
            }
        }
    </style>
{% endblock %}
